<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lwx.serviceorder.mapper.OrderMapper">
    <select id="findList" resultType="com.lwx.internalcommon.dto.OrderInfo">
        select * from order_info t
        order by t.id desc
    </select>
</mapper>