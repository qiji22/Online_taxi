<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lwx.serviceDriverUser.mapper.DriverUserMapper">
    <select id="findList" resultType="com.lwx.internalcommon.dto.DriverUser">
        select * from service_driver_user t
        order by t.id desc
    </select>
</mapper>